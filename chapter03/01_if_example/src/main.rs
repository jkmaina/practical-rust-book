// This is a simple Rust program that checks if a number is greater than 5
// and prints a message if it is.
// The program uses an if statement to determine the output.

fn main() {
    let number = 7;
    
    if number > 5 {
        println!("The number {} is greater than 5", number);
    }
}
    