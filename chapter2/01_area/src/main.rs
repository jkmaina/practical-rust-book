// This code shows variables by string values and
// calculating the area of a rectangle given its length and width.
fn main() {
    let length = 5;
    let width = 3;
    let area = length * width;
    println!("The area of the rectangle is: {}", area);
}
