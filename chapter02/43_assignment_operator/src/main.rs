// This Rust program demonstrates the use of the assignment operator `=`
// to change the value of a variable.
// It initializes a variable, modifies it, and prints the updated value.
fn main() {
    let mut x = 5;
    println!("x is initially {}", x);
    
    x = 10;
    println!("x is now {}", x);
}
